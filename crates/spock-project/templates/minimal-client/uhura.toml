[project]
name = "spock.starter"
version = 1
language = "0.4"

[modules]
starter = "machine.uhura"
ui = "ui.uhura"

[evidence]
sources = ["evidence.uhura"]

# Live machine, presentation, lifetime, and port bindings belong to host.toml.
# Add [assets] or [icons] here when the application needs them.
